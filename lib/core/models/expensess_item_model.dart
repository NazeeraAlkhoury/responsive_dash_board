class ExpensessItemModel {
  final String image, title, date, amount;

  const ExpensessItemModel(
      {required this.image,
      required this.title,
      required this.date,
      required this.amount});
}
